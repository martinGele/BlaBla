<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/main_background"
    android:orientation="vertical" >

    <ImageView
        android:id="@+id/header_img"
        android:layout_width="match_parent"
        android:layout_height="@dimen/header_img_height"
        android:background="@drawable/header_faq" />        

    <WebView
        android:id="@+id/faq_webview"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/header_img"
        android:scrollbars="none"
        android:textSize="18dp" />

    <RelativeLayout
        android:id="@+id/emailBar"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginTop="@dimen/margin_top_thirty"
        android:background="@color/orange_color" >

        <TextView
            android:id="@+id/faqEmail"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginBottom="@dimen/margin_top_twenty"
            android:layout_marginTop="@dimen/margin_top_twenty"
            android:text="@string/contact_us_faq"
            android:textColor="@color/white" />
    </RelativeLayout>

</RelativeLayout>